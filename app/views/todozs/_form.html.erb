<%= form_with(model: todoz) do |form| %>
  <% if todoz.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(todoz.errors.count, "error") %> prohibited this todoz from being saved:</h2>

      <ul>
        <% todoz.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :task, style: "display: block" %>
    <%= form.text_field :task %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
